<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.back.mapper.UmsMemberMapper">

    <resultMap id="BaseResultMap" type="com.example.back.entity.ums.UmsMember">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="balance" property="balance"/>
        <result column="member_level" property="memberLevel"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO ums_member (user_id, balance, member_level)
        VALUES (#{userId}, #{balance}, #{memberLevel})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, user_id, balance, member_level
        FROM ums_member
        WHERE id = #{id}
    </select>

    <select id="selectByUserId" resultMap="BaseResultMap">
        SELECT id, user_id, balance, member_level
        FROM ums_member
        WHERE user_id = #{userId}
    </select>

    <update id="updateBalance">
        UPDATE ums_member
        SET balance = #{balance}
        WHERE id = #{id}
    </update>

</mapper>
