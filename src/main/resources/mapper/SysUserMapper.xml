<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.back.mapper.SysUserMapper">

    <resultMap id="BaseResultMap" type="com.example.back.entity.sys.SysUser">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="nickname" property="nickname"/>
        <result column="phone" property="phone"/>
        <result column="avatar" property="avatar"/>
        <result column="user_type" property="userType"/>
        <result column="openid" property="openid"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_user (username, password, nickname, phone, avatar, user_type, openid, status)
        VALUES (#{username}, #{password}, #{nickname}, #{phone}, #{avatar}, #{userType}, #{openid}, #{status})
    </insert>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT id, username, password, nickname, phone, avatar, user_type, openid, status, create_time, update_time
        FROM sys_user
        WHERE id = #{id}
    </select>

    <select id="selectByUsername" resultMap="BaseResultMap">
        SELECT id, username, password, nickname, phone, avatar, user_type, openid, status, create_time, update_time
        FROM sys_user
        WHERE username = #{username}
    </select>

    <select id="selectByPhone" resultMap="BaseResultMap">
        SELECT id, username, password, nickname, phone, avatar, user_type, openid, status, create_time, update_time
        FROM sys_user
        WHERE phone = #{phone}
    </select>

    <select id="selectByOpenid" resultMap="BaseResultMap">
        SELECT id, username, password, nickname, phone, avatar, user_type, openid, status, create_time, update_time
        FROM sys_user
        WHERE openid = #{openid}
    </select>

    <update id="updateUserType">
        UPDATE sys_user SET user_type = #{userType} WHERE id = #{id}
    </update>
</mapper>
