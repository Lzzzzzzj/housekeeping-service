<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.back.mapper.SysConfigMapper">

    <resultMap id="BaseResultMap" type="com.example.back.entity.sys.SysConfig">
        <id column="id" property="id"/>
        <result column="config_key" property="configKey"/>
        <result column="config_value" property="configValue"/>
        <result column="remark" property="remark"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectByKey" resultMap="BaseResultMap">
        SELECT id, config_key, config_value, remark, update_time
        FROM sys_config
        WHERE config_key = #{configKey}
        LIMIT 1
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_config (config_key, config_value, remark)
        VALUES (#{configKey}, #{configValue}, #{remark})
    </insert>

    <update id="updateValue">
        UPDATE sys_config
        SET config_value = #{configValue}
        WHERE config_key = #{configKey}
    </update>

</mapper>

